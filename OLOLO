#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n=0,p=0,ans=0;    scanf("%d",&n);
    while(n--)
    {
        scanf("%d",&p);
        ans^=p;
    }
    printf("%d",ans);
    return 0;
}
