#include<bits/stdc++.h>
using namespace std;
int main()
{
	int num[100],i,temp;
	for(i=0;i<100;i++)
	{
		scanf("%d",&num[i]);
		if(num[i]==42)
		{
			scanf("%d",&num[i+1]);
			temp=i;
			break;
		}
	}
	for(i=0;i<temp;i++)
		printf("%d\n",num[i]);
	return 0;
}
