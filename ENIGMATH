#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t=0;    scanf("%d",&t);
    while(t--)
    {
        long long int a,b,g;  scanf("%lld %lld",&a,&b);
        g=__gcd(a,b);
        cout<<b/g<<" "<<a/g<<endl;
    }
    return 0;
}
