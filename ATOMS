#include<iostream>
#include<cmath>
using namespace std;
int main()
{
    int p=0;    cin>>p;
    long long int n,k,m;
    while(p--)
    {
        float t=0;
        cin>>n>>k>>m;
        if(m<=n)
            t=0;
        else
            t=log(m/n)/log(k);
        cout<<(int)t<<endl;
    }
} 
